package edu.andreasgut.labyrinth.core;

import java.util.LinkedList;

public class CustomSolver {

    // TODO: Impelentiere die Methode solve() so, dass sie in einer LinkedList alle Positionen speichert,
    //  die auf dem Weg zum Ziel zurückgelegt werden. Zum Schluss wird die Liste zurückgegeben.
    public static LinkedList<Position> solve(boolean[][] labyrinthArray, Position start, Position goal) {

        LinkedList<Position> solution = new LinkedList<>();

        return solution;

    }
}
